---
'roboport': minor
---

Rename model adapter classes to drop the `Model` suffix: `AnthropicModel` → `Anthropic`, `OpenAIModel` → `OpenAI`, `GeminiModel` → `Gemini`, `MoonshotModel` → `Moonshot`, and `OpenAICompatibleModel` → `OpenAICompatible`. Update imports from `roboport/models` accordingly.
