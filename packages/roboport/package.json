{
  "name": "roboport",
  "version": "0.4.0",
  "description": "Minimal TypeScript framework for building LLM agents.",
  "author": {
    "name": "Timur Badretdinov",
    "url": "https://github.com/Destiner"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Destiner/roboport.git",
    "directory": "packages/roboport"
  },
  "homepage": "https://github.com/Destiner/roboport#readme",
  "bugs": {
    "url": "https://github.com/Destiner/roboport/issues"
  },
  "keywords": [
    "llm",
    "agent",
    "ai",
    "mcp"
  ],
  "engines": {
    "bun": ">=1.0.0"
  },
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./gateways": "./src/gateways/index.ts",
    "./harness": "./src/harness/index.ts",
    "./mcp": "./src/mcp/index.ts",
    "./models": "./src/models/index.ts",
    "./skills": "./src/skills/index.ts",
    "./triggers": "./src/triggers/index.ts"
  },
  "scripts": {
    "check": "bun run check:prettier && bun run check:eslint",
    "check:prettier": "prettier -c \"**/*.{json,js,ts}\"",
    "check:eslint": "eslint \"**/*.{js,ts}\"",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "rm -rf dist && bun run build:js && bun run build:types && bun run build:manifest",
    "build:js": "bun scripts/build-js.ts",
    "build:types": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json --resolve-full-paths",
    "build:manifest": "bun scripts/build-manifest.ts",
    "dist:pack": "bun run build && cd dist && bun pm pack --destination .."
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "tsc-alias": "^1.8.17",
    "zod": "^4.4.3"
  }
}
